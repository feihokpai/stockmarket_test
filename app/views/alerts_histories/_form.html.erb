<%= form_with(model: alerts_history) do |form| %>
  <% if alerts_history.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(alerts_history.errors.count, "error") %> prohibited this alerts_history from being saved:</h2>

      <ul>
        <% alerts_history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
